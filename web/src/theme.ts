
import { definePreset } from '@primeuix/themes';
import Aura from '@primeuix/themes/aura';

export const my_preset_theme = definePreset(Aura, {
    semantic: {
        colorScheme: {
            light: {
            },
            dark: {
            }
        }
    }
});
