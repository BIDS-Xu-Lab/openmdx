[project]
name = "api"
version = "0.1.0"
description = "AI Agent-based diagnostic service"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.119.1",
    "smolagents[mcp]>=1.22.0",
]
